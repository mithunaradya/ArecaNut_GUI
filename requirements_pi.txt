# Areca Nut Ripeness Detection - Raspberry Pi Requirements
# Optimized for Raspberry Pi 4 with camera module

# Core Dependencies (Required for Pi)
# ===================================

# Computer Vision & Camera
opencv-python==4.8.1.78
Pillow>=9.0.0

# Machine Learning (Traditional ML only - no TensorFlow for Pi)
scikit-learn>=1.3.0
numpy>=1.21.0

# System Dependencies (install via apt)
# ====================================
# sudo apt update
# sudo apt install -y python3-pip python3-dev
# sudo apt install -y libopencv-dev python3-opencv
# sudo apt install -y libjpeg-dev libtiff5-dev libpng-dev
# sudo apt install -y libavcodec-dev libavformat-dev libswscale-dev
# sudo apt install -y libgtk2.0-dev libcanberra-gtk-module
# sudo apt install -y libxvidcore-dev libx264-dev
# sudo apt install -y libatlas-base-dev gfortran

# GUI Framework
# =============
# tkinter - Built-in with Python (no installation needed)

# Optional: Audio feedback
# =======================
# sudo apt install -y espeak espeak-data

# Installation Commands for Pi:
# ============================
# 1. System packages:
#    sudo apt update && sudo apt upgrade -y
#    sudo apt install -y python3-pip python3-opencv libatlas-base-dev
#
# 2. Python packages:
#    pip3 install -r requirements_pi.txt
#
# 3. Test camera:
#    python3 -c "import cv2; print('OpenCV version:', cv2.__version__)"
#
# 4. Run application:
#    python3 Areca_Pi.py

# Performance Notes for Pi:
# ========================
# - Uses only traditional ML models (KNN, Random Forest, SVC)
# - No TensorFlow (too heavy for Pi)
# - Optimized camera preview (lower resolution/FPS)
# - Smaller GUI elements for touchscreen
# - Efficient memory usage
# - Expected accuracy: 98.7% (same as full system)

# Hardware Requirements:
# =====================
# - Raspberry Pi 4 (2GB+ RAM recommended)
# - USB Camera or Pi Camera Module
# - MicroSD card (16GB+ recommended)
# - Optional: 7" touchscreen display